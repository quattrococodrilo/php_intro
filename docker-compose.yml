version: "3.3"
services:
    web:
        build:
            context: .
            dockerfile: compose/Dockerfile
        ports:
            - "80:80"      
        volumes:
            - ./app/htdocs:/opt/lampp/htdocs
        command: "/opt/lampp/lampp start"
